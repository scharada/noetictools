The sole purpose of the NoeticTools.BuildInfo project is to generate a AssemblyInfo.cs
that includes the Subversion repository revision number as the build number.

This is done by a MSBuild task: MSBuildCommunityTasks\MSBuild.Community.Tasks.Targets.

Configured in the project file.